alias nv=nvim
alias ff=fastfetch
alias zj="zellij a -c"
alias top=btop

alias l="ls -l"
alias la="ls -la"

alias ..="cd .."
alias ...="cd ../.."

# Progs
eval "$(zoxide init bash)"
eval "$(starship init bash)"

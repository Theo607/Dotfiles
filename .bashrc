alias nv=nvim
alias ff=fastfetch
alias zj="zellij a -c"
alias top=btop

alias l="ls -l"
alias la="ls -la"

alias ..="cd .."
alias ...="cd ../.."

alias ga="git add"
alias gcm="git commit -m"
alias gp="git push"
alias gpo="git push origin"

eval "$(zoxide init bash)"
eval "$(starship init bash)"
